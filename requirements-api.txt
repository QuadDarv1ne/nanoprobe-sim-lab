# FastAPI API dependencies for Nanoprobe Sim Lab
# Дополнительные зависимости для REST API

# FastAPI и сервер
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Аутентификация
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
# Argon2 - победитель Password Hashing Competition (рекомендован OWASP)
# passlib включает argon2 через argon2-cffi
passlib[argon2,bcrypt]>=1.7.4
argon2-cffi>=23.1.0
bcrypt==4.0.1  # Compatible version with passlib (fallback)

# Валидация
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# WebSocket
websockets>=12.0

# Кэширование
redis>=5.0.0

# HTTP клиент (для NASA API)
aiohttp>=3.9.0

# Rate Limiting
slowapi>=0.1.8

# Для production
gunicorn>=21.2.0
aiofiles>=23.2.1

# Мониторинг (опционально)
prometheus-client>=0.19.0
sentry-sdk[fastapi]>=1.39.0
psutil>=5.9.0

# Миграции БД
sqlalchemy>=2.0.0
alembic>=1.13.0
