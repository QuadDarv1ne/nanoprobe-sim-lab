perf: add HTTP session with connection pooling and retry

Performance optimization for external services:

- Create shared HTTP session with connection pooling
- Add retry strategy with exponential backoff
- Configure retry for 429, 500, 502, 503, 504 status codes
- Pool connections: 10 per host, max 20 total
- Use session instead of raw requests.get()
- Add os import for NASA_API_KEY env variable

Benefits:
- Reduced connection overhead
- Automatic retry on transient failures
- Better resource utilization
- Improved reliability
