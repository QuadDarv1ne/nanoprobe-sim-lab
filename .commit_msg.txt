feat: add centralized error handling for API

Critical improvement #2 - Unified error handling:

- Create api/error_handlers.py with centralized exception handlers
- Add custom exception classes:
  - APIError (base)
  - ValidationError, NotFoundError, AuthenticationError
  - AuthorizationError, RateLimitError, DatabaseError
  - ExternalServiceError
- Add ErrorSeverity enum for categorization
- Register handlers in main.py
- Structured logging with error_id, context, severity
- Consistent error response format across all endpoints
- Production-safe error messages (hide details by default)
