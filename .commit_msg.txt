feat: Redis integration for refresh token storage

Critical improvement - Redis integration:

- Add _get_redis_client() for Redis connection
- Add _store_refresh_token() - save jti to Redis with TTL
- Add _is_token_valid() - check token validity via Redis
- Add _revoke_refresh_token() - delete token from Redis
- Fallback to in-memory storage if Redis unavailable
- Update refresh_access_token to use Redis validation
- Add logging for token operations

Benefits:
- Persistent token storage across restarts
- Automatic token expiration via Redis TTL
- Distributed token validation (multi-instance)
- Graceful degradation to in-memory
